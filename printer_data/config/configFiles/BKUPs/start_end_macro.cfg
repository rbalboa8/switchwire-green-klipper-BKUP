[gcode_macro START_PRINT]
gcode:
    # Accept either standard or DKEU style parameter names with sensible fallbacks
    {% set BED_TEMP = params.BED_TEMP|default(params.BED|default(60))|float %}
    {% set EXTRUDER_TEMP = params.EXTRUDER_TEMP|default(params.EXTRUDER|default(200))|float %}

    # 1. Reset metrics & coordinate modes
    M220 S100 ; Reset speed factor
    M221 S100 ; Reset flow rate
    G90       ; Absolute positioning
    M83       ; Relative extruder mode

    # 2. Start bed heating and preheat nozzle to 150C (prevents oozing while meshing)
    SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET={BED_TEMP}
    SET_HEATER_TEMPERATURE HEATER=extruder TARGET=150

    # 3. Home all axes
    G28

    # 4. Wait for bed to reach final printing temperature
    TEMPERATURE_WAIT SENSOR=heater_bed MINIMUM={BED_TEMP - 1} MAXIMUM={BED_TEMP + 5}

    # 5. Adaptive Bed Mesh (KAMP or native Klipper)
    BED_MESH_CLEAR
    BED_MESH_CALIBRATE ADAPTIVE=1

    # 6. Heat nozzle to target printing temperature
    SET_HEATER_TEMPERATURE HEATER=extruder TARGET={EXTRUDER_TEMP}
    TEMPERATURE_WAIT SENSOR=extruder MINIMUM={EXTRUDER_TEMP - 2} MAXIMUM={EXTRUDER_TEMP + 5}

    # 7. Purge line (Adaptive purge if KAMP is loaded, or standard fallback purge)
    {% if 'gcode_macro LINE_PURGE' in printer %}
        LINE_PURGE
    {% else %}
        G1 Z2.0 F3000
        G1 X5 Y10 Z0.3 F5000.0
        G1 X5 Y150.0 Z0.3 F1500.0 E15
        G1 X5.4 Y150.0 Z0.3 F5000.0
        G1 X5.4 Y10 Z0.3 F1500.0 E30
        G92 E0
        G1 Z2.0 F3000
    {% endif %}


[gcode_macro END_PRINT]
gcode:
    # Retract slightly to prevent oozing
    G91
    G1 E-2 F2700
    G1 E-1.5 Z0.2 F2400
    G1 X5 Y5 F3000
    G1 Z10 F3000
    G90

    # Park toolhead at rear
    G1 X10 Y200 F6000

    # Turn off heaters and fans
    TURN_OFF_HEATERS
    M107
    M84 X Y E ; Disable steppers (keep Z engaged if needed)
    BED_MESH_CLEAR